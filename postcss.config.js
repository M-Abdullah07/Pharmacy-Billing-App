import tailwindcss from '@tailwindcss/postcss'
import autoprefixer from 'autoprefixer'

export default {
  plugins: [
    tailwindcss,
    autoprefixer,
  ],
}
// This is a PostCSS configuration file that uses Tailwind CSS and Autoprefixer.
// It exports an object with a plugins array that includes the Tailwind CSS and Autoprefixer plugins.
// Tailwind CSS is a utility-first CSS framework, and Autoprefixer is a Post